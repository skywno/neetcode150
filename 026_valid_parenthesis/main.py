class Solution:
    def isValid(self, s: str) -> bool:
        opening_brackets = {'(', '{', '['}
        arr = []
        for _ in s: 
            if _ in opening_brackets:
                arr.append(_)
            else
                

